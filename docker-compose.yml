version: '3'
services:
  web:
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - .:/usr/src/app
    expose:
      - '80'
    ports:
      - '80:80'
    command: ['nginx', '-g', 'daemon off;']
